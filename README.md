# openclaw docker
Deploy openclaw with docker

# deploy
```bash
# first time
./start.sh -i

# non-first time start
./start.sh

# stop
./stop.sh

# find home path of openclaw
./openclaw_home_path.sh

```
